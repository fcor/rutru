import React from 'react'
import Button from './Button'

const NextStep = () =>
  <section className="next column">
    <h1 className="next-title">
      ¿Estas listo para dar el primer paso?
    </h1>
    <Button type="transparent">
      Contáctanos
    </Button>
  </section>

export default NextStep
